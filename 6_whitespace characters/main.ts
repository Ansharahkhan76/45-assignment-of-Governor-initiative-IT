let WhiteSpace_Name = "\n\tAnshrah Khan \t\n";
console.log(WhiteSpace_Name); // Anshrah Khan 

// Trimming white spaces from the
let Without_WhiteSpace =WhiteSpace_Name.trim( );  //Removing the white spaces from both
console.log(Without_WhiteSpace)